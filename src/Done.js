import React from 'react';

class Done extends React.Component {
    constructor(props) {
        super(props);
    }

    render = () => (
        <span> "Mark us DONE"</span>
    );
}

export default Done;
